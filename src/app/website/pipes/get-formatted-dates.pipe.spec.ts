import { GetFormattedDatesPipe } from './get-formatted-dates.pipe';

describe('GetFormattedDatesPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFormattedDatesPipe();
    expect(pipe).toBeTruthy();
  });
});
