import { GetFormattedDatesPipePipe } from './get-formatted-dates-pipe.pipe';

describe('GetFormattedDatesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GetFormattedDatesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
